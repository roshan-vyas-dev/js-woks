
var numbers = [10, 11, 12, 13, 14, 15, 16];

// addfives
// squares
// cubes

var addFives=numbers.map(num=>num+5);
console.log(addFives);

var squares=numbers.map(num=>num**2);
console.log(squares);